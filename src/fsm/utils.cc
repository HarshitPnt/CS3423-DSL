#include "../../includes/fsm.hh"

namespace fsm
{
    dfa reg_to_dfa(const regex reg)
    {
    }

    nfa reg_to_nfa(const regex reg)
    {
    }

    dfa nfa_to_dfa(const nfa n)
    {
    }

    pda cfg_to_pda(const cfg c)
    {
    }
} // namespace fsm